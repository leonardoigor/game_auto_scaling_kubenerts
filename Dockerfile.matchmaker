FROM node:20-alpine
WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm ci --only=production || npm install --only=production
COPY matchmaker ./matchmaker
COPY common ./common
ENV MATCHMAKER_PORT=4000
CMD ["node","matchmaker/index.js"]
